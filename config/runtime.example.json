{
  "backend": "scripted",
  "gameplayTarget": "continuous",
  "testMode": "pause-at-decision",
  "decisionTimeoutMs": 5000,
  "paidInferenceEnabled": false,
  "note": "Planning example; no code loads this file. Live inference runs only through finite trial ledgers (see docs/EVALUATION.md). Pump limits do not replace billing quotas.",
  "attention": {
    "cooldownTicks": 300,
    "timeoutMs": 5000,
    "maxConcurrent": 2,
    "maxTurns": 12
  }
}
